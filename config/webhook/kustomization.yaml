resources:
- manifests.yaml
- service.yaml

commonAnnotations:
  service.beta.openshift.io/inject-cabundle: "true"

configurations:
- kustomizeconfig.yaml

patches:
- path: patches/connection-secret-match-conditions.yaml
